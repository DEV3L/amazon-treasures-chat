[
  { "type": "file_search" },
  {
    "type": "function",
    "function": {
      "name": "RETRIEVE_PRODUCTS",
      "description": "Call this function to return details about a user's selected products",
      "parameters": {
        "type": "object",
        "properties": {
          "ids": {
            "type": "array",
            "description": "A list of 'asin' values from the uploaded retrieval context",
            "items": {
              "type": "string"
            }
          }
        },
        "required": ["ids"]
      }
    }
  }
]
